import React from 'react';

import './styles.css';

function DeckId() {

  // Rota #2 - /deck/<<deck_id>>
  // Esta página deve carregar a pilha criada pela Rota #1, usando o deck_id dado pelo endpoint "A Brand New Deck" [1]
  // mostrar A pilha de cartas em ordem
  // mostrar A Carta de Rotação
  // mostrar A Carta de Maior Valor
  // mostrar Todas as combinações Full House


  return (
    <div>
        <p>DeckId</p>
    </div>
  );
}

export default DeckId;